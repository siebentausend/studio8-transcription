# requirements.txt
# Install with: pip install -r requirements.txt
# Note: PyTorch must be installed separately with the correct CUDA version.
# See SETUP.md for instructions.

# Transcription
whisperx>=3.1.1
faster-whisper>=1.0.0

# Web GUI
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Watchfolder (local filesystem monitoring)
watchdog>=4.0.0

# Audio/video metadata (ffprobe wrapper — ffmpeg must be installed separately)
ffmpeg-python>=0.2.0

# Configuration
pyyaml>=6.0

# Diarization post-processing
pandas>=2.0.0
